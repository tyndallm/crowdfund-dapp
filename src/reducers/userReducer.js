
const initialState = {
    isFetching: false,
    accounts: [],
    coinbase: "",
    selectedAccount: 0
}

export default function userReducer(state = {}, action) {
    switch (action.type) {
    default:
      return state
    }
}